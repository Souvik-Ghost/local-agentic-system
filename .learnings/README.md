# Local Agent Learnings

This directory captures persistent insights, unexpected operation failures, and user corrections. 

## Extraction Workflow
When an error is recurring:
1. The agent distills the solution.
2. The agent catalogs the error and the fix in this folder.
3. The agent compiles it into an operational rule (e.g., added to workspace memory).
