# Structured Vault (Layer 3)

This directory is an Obsidian repository serving as the permanent documentation and strategic guide for the local agent.

## Integration
- Markdowns placed here represent the durable project knowledge.
- The `src/memory_engine` parses these documents to extract text embeddings, indexing them into ChromaDB for semantic search access during the agent loop.
